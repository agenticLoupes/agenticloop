// [OWNER: C]  Canvas over the video: boxes, labels, the highlight circle.
//
// Box coords arrive normalized 0-1, origin top-left. Scale to the canvas; you
// never need the camera resolution (plan.md §7.1).
// `highlight: true` -> animate the circle on.
