export default function Banner() {
  return (
    <div className="sticky top-0 z-50 w-full border-b border-amber-200 bg-amber-50 px-4 py-2 text-center text-[13px] font-medium text-amber-900">
      Demo — synthetic patient data only. Never use with real records.
    </div>
  );
}
